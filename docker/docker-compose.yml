services:
  action:
    image: public.ecr.aws/infra-blocks/docker-action-template:latest
    build:
      context: ../
      dockerfile: docker/action/Dockerfile
    container_name: docker-action-template
